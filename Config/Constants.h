#pragma once
#include <GLFW/glfw3.h>

namespace Constants
{
    inline constexpr int ScreenWidth = 800;
    inline constexpr int ScreenHeight = 600;

    // Input keys
    inline constexpr int KeyLeft = GLFW_KEY_LEFT;
    inline constexpr int KeyRight = GLFW_KEY_RIGHT;
}
